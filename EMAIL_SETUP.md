# 邮件通知功能使用说明

## 功能概述

币安价格监控系统现已支持邮件通知功能。当价格告警触发时，除了浏览器通知外，还可以发送邮件到指定邮箱。

## 配置步骤

### 1. 打开邮件配置

在主页面右上角点击 **"邮件配置"** 按钮。

### 2. 填写邮件配置信息

#### 基本配置
- **启用邮件通知**: 勾选此选项以启用邮件功能
- **SMTP 服务器地址**: 邮件服务商的 SMTP 服务器（如 QQ 邮箱：`smtp.qq.com`）
- **SMTP 端口**: 通常为 `587`（TLS）或 `465`（SSL）

#### 发件邮箱配置
- **发件邮箱**: 用于发送通知邮件的邮箱地址
- **邮箱密码/授权码**: 
  - ⚠️ **重要**: QQ 邮箱、163 邮箱等需要使用**授权码**，不是登录密码
  - 授权码获取方法见下方说明

#### 收件配置
- **收件邮箱**: 接收价格告警通知的邮箱地址
- **邮件主题前缀**: 邮件主题的前缀标识（默认：`[币安提醒]`）
- **邮件发送间隔**: 防止频繁发送，同一告警在此时间内只发送一次（默认：5 分钟）

### 3. 测试配置

点击 **"测试配置"** 按钮，系统会发送一封测试邮件到收件邮箱。如果配置正确，您将收到测试邮件。

### 4. 保存配置

确认配置无误后，点击 **"保存配置"** 按钮。

## 获取邮箱授权码

### QQ 邮箱授权码获取步骤

1. 登录 [QQ 邮箱网页版](https://mail.qq.com)
2. 点击顶部 **"设置"** → **"账户"**
3. 找到 **"POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务"** 部分
4. 开启 **"IMAP/SMTP服务"** 或 **"POP3/SMTP服务"**
5. 按照提示发送短信验证
6. 验证成功后，系统会显示**授权码**（16位字符）
7. 复制授权码并粘贴到邮件配置的 **"邮箱密码/授权码"** 字段

### 163 邮箱授权码获取步骤

1. 登录 [163 邮箱网页版](https://mail.163.com)
2. 点击顶部 **"设置"** → **"POP3/SMTP/IMAP"**
3. 开启 **"IMAP/SMTP服务"**
4. 点击 **"客户端授权密码"** 后的 **"开启"**
5. 按照提示验证手机号
6. 设置授权密码（16位字符）
7. 将授权密码填入邮件配置

### Gmail 授权码获取步骤

1. 开启两步验证
2. 访问 [Google 应用专用密码](https://myaccount.google.com/apppasswords)
3. 选择应用和设备
4. 生成密码
5. 使用生成的 16 位密码

## 使用邮件通知

### 创建带邮件通知的告警

1. 在交易对卡片上点击 **"告警设置"** 按钮
2. 填写告警信息（价格类型、条件、目标价格）
3. 勾选 **"同时发送邮件通知"** 选项
4. 点击 **"添加告警"** 按钮

### 邮件通知内容

当价格触发告警时，您将收到包含以下信息的邮件：
- 交易对名称
- 价格类型（合约/现货）
- 触发条件
- 目标价格
- 当前价格
- 触发时间

## 常见问题

### Q: 为什么测试邮件发送失败？

**A:** 请检查以下几点：
1. 确认使用的是**授权码**而不是登录密码
2. 确认 SMTP 服务器地址和端口正确
3. 确认发件邮箱已开启 SMTP 服务
4. 检查网络连接是否正常

### Q: 为什么没有收到告警邮件？

**A:** 可能的原因：
1. 邮件配置未启用（检查 "启用邮件通知" 是否勾选）
2. 告警创建时未勾选 "同时发送邮件通知"
3. 邮件在节流时间内（默认 5 分钟内同一告警只发送一次）
4. 邮件被邮箱服务商拦截（检查垃圾邮件文件夹）

### Q: 可以使用其他邮箱服务商吗？

**A:** 可以。只需要填写对应的 SMTP 服务器信息：

| 邮箱服务商 | SMTP 服务器 | 端口 | 需要授权码 |
|-----------|------------|------|-----------|
| QQ 邮箱 | smtp.qq.com | 587/465 | 是 |
| 163 邮箱 | smtp.163.com | 465 | 是 |
| Gmail | smtp.gmail.com | 587 | 是 |
| Outlook | smtp-mail.outlook.com | 587 | 否 |
| 阿里云邮箱 | smtp.aliyun.com | 465 | 是 |

### Q: 邮件配置保存在哪里？

**A:** 邮件配置保存在浏览器的 localStorage 中，仅在本地存储，不会上传到服务器。

## 安全提示

⚠️ **重要安全提示**：
- 邮箱授权码具有与密码相同的权限，请妥善保管
- 不要将授权码分享给他人
- 如果授权码泄露，请立即在邮箱设置中重置
- 建议使用专门的邮箱账号用于发送通知

## 技术说明

- 邮件发送使用 Nodemailer 库
- 支持 TLS/SSL 加密连接
- 邮件配置存储在浏览器本地
- 邮件发送通过 Next.js API 路由处理
- 自动节流机制防止频繁发送

## 默认配置文件

系统提供了默认配置文件模板：`config/email.config.js`

您可以根据需要修改此文件，但建议通过 UI 界面进行配置，更加安全和方便。
